# Dependencies
node_modules/

# Build output
dist/

# Environment / secrets (the intelligence layer is opt-in via aios/.env)
.env
.env.*
!.env.example

# Phase-4 local store + model cache (regenerated by `npm run kb:index`)
.kb-index/
.cache/

# OS / editor
.DS_Store
Thumbs.db
